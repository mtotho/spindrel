name: Chip — Toggle
version: 1.0.0
description: Reference chip widget — label + click-to-toggle switch that persists via spindrel.state.
layout_hints:
  preferred_zone: chip
  min_cells:
    w: 2
    h: 1
  max_cells:
    w: 4
    h: 1
