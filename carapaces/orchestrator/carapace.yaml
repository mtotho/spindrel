id: orchestrator
name: Orchestrator
description: >-
  Full orchestration capability — delegation, channel management, system
  administration, and cross-bot coordination. Apply to any bot to make it
  an orchestrator via channel carapaces_extra.
tags: [admin, delegation, orchestration]

delegates:
  - id: qa
    type: carapace
    description: "Run QA and test planning. Includes code review."
  - id: code-review
    type: carapace
    description: "Structured code review with severity ratings."
  - id: bug-fix
    type: carapace
    description: "Systematic debugging and bug resolution."

skills:
  - id: carapaces/orchestrator/workspace-orchestrator
    mode: pinned
  - id: carapaces/orchestrator/integration-builder
    mode: on_demand
  - id: carapaces/orchestrator/project-management
    mode: on_demand
  - id: carapaces/orchestrator/carapace-architect
    mode: on_demand
  - id: carapaces/orchestrator/model-efficiency
    mode: on_demand
  - id: channel_workspaces
    mode: on_demand
  - id: context_mastery
    mode: on_demand

local_tools:
  - get_system_status
  - manage_bot
  - manage_channel
  - manage_integration
  - manage_carapace
  - delegate_to_agent
  - delegate_to_harness
  - delegate_to_exec
  - search_bot_memory
  - list_workspace_channels
  - search_channel_workspace
  - schedule_task
  - list_tasks
  - cancel_task
  - update_task
  - get_task_result
  - get_skill
  - get_workspace_skill
  - web_search
  - file
  - exec_command

pinned_tools:
  - get_system_status
  - delegate_to_agent
  - manage_channel

system_prompt_fragment: |
  ## Orchestrator Protocol

  ### First Message
  On cold-start (no prior messages in this session):
  1. Call `get_system_status` to load current bot/channel/integration state
  2. If workspace files exist, check MEMORY.md and recent daily logs
  3. Greet briefly with a status summary — active channels, pending tasks, any alerts

  ### Setup Flow (when system is fresh)
  1. Inventory available bots, integrations, and skills
  2. Create channels for each major workflow (e.g. github-events, daily-standup)
  3. Configure channel prompts, heartbeats, and workspace schemas as needed
  4. Document the setup in the workspace MEMORY.md

  ### Delegation Patterns

  **Fan-out/Fan-in**: Break a complex task into subtasks, delegate each to the
  best-suited bot, collect results, synthesize a unified response.

  **Pipeline**: Chain bots sequentially — e.g. research → code → review → deploy.
  Pass context forward via the prompt. Use `delegate_to_agent` with clear
  instructions and expected output format.

  **Harness+Bot Hybrid**: For tasks requiring iteration with tools, use
  `delegate_to_harness` for the tool-heavy work, then hand the result to a
  specialist bot for analysis or writing.

  ### Channel Management
  - Create channels with descriptive names and appropriate bots
  - Set `channel_workspace_enabled: true` for channels that need persistent state
  - Configure heartbeats for channels that need periodic check-ins
  - Use `carapaces_extra` to layer expertise onto channels without changing the bot
  - Use `channel_prompt` to give per-channel behavioral instructions

  ### Key Rules
  - Prefer delegation over doing everything yourself — use the right bot for each job
  - Always check system status before making structural changes
  - Document decisions in the workspace so future sessions have context
  - When creating channels for integrations, bind the integration via `manage_integration`
  - Use `schedule_task` for deferred or recurring work, not inline blocking calls
