name: "agent-server"
services:
  agent-server:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    environment:
      - FASTEMBED_CACHE_DIR=/app/.fastembed-cache
      - WORKSPACE_LOCAL_DIR=/workspace-data
      - HOME_LOCAL_DIR=/app/home
      - SPINDREL_INSTANCE_ID=local
    volumes:
      - ./.env:/app/.env                                    # settings sync (SPINDREL_HOME → HOME_HOST_DIR)
      - ./bots:/app/bots
      - ./skills:/app/skills
      - ./tools:/app/tools
      # MCP servers are managed via Admin UI.
      # Uncomment to seed from mcp.yaml on first boot (file must exist on host):
      # - ./mcp.yaml:/app/mcp.yaml:ro
      - ${HOME_HOST_DIR:-./_no_home}:/app/home:ro           # Spindrel home dir (integrations, external assets)
      - /var/run/docker.sock:/var/run/docker.sock        # integration sidecar containers
      - ${WORKSPACE_HOST_DIR:-~/.spindrel-workspaces}:/workspace-data  # workspace files
      - fastembed-cache:/app/.fastembed-cache  # local embedding model cache
      # Persistent install cache — survives `spindrel pull` rebuilds.
      # See app/routers/api_v1_admin/install_cache.py for the admin reset.
      - spindrel-home:/home/spindrel                   # npm-global, pip cache, playwright, HF/whisper caches, agent ~/.local
      - spindrel-apt-archives:/var/cache/apt/archives  # cached .deb downloads (feed for dpkg-x installs below)
      - spindrel-pkg:/opt/spindrel-pkg                 # dpkg-extracted package tree (chromium, gh, etc.) — binaries persist here
      # Config state auto-export: uncomment after first run creates the file
      # (bind-mounting a non-existent file makes Docker create a directory)
      # - ./config-state.json:/app/config-state.json
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: agent
      POSTGRES_PASSWORD: agent
      POSTGRES_DB: agentdb
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U agent -d agentdb"]
      interval: 5s
      timeout: 3s
      retries: 5
    restart: unless-stopped

volumes:
  pgdata:
  fastembed-cache:
  spindrel-home:
  spindrel-apt-archives:
  spindrel-pkg:
