---
tags: [agent-server, track, projects]
status: active
updated: 2026-04-30
---

# Track - Projects

Evergreen scope for first-class Project roots inside the singleton SharedWorkspace. Projects are named workspace-relative roots that multiple channels can attach to; they are not replacement workspaces and are not limited to one Git repo.

## Invariants

- `projects` owns the reusable root, prompt settings, and metadata; `channels.project_id` is the primary binding.
- `projects.WorkSurface` is the shared policy object for root path, display path, index root, workspace search prefix, knowledge prefix, Project prompt, and channel/project identity.
- Project-bound turns use the Project root for files, search, terminal, exec, harness cwd, context injection, and channel workspace search.
- Project-bound failures fail visibly. A broken Project binding or wrong-Project fresh instance must not silently fall back to the channel or bot workspace.
- Project Instances are disposable Project roots created from an applied Blueprint snapshot. They reuse the parent Project work-surface policy and are selected by session binding or run-scoped task context, not by mutating the channel Project binding.
- Bot-private workspace-files memory remains separate and is written through the dedicated `memory` tool.
- Project-scoped knowledge lives under `.spindrel/knowledge-base` inside the Project root.
- Project runtime env is derived from the applied Blueprint snapshot plus Project secret bindings. UI/API surfaces expose key names and readiness only; values are injected process-local into Project terminals, exec, and harness turns and are redacted from tool/harness event output.

## Phase Log

- [x] **Phase 1 - First-class primitive** — Projects API/model, channel binding, Project-rooted file/search/terminal/exec/harness/context behavior, Project prompt injection, and compatibility fallback for legacy `project_path` shipped. Architecture locked in [[Architecture Decisions#Projects are shared roots inside the singleton Workspace]].
- [x] **Phase 1.5 - UI repair** — Project list/detail/channel-settings moved to low-chrome admin rows/sections and the screenshot bundle verifies the memory tool envelope remains visible.
- [x] **Phase 2A - Workspace surface** — Project detail became the actual work surface: embedded Project-root file browser, inline Project-root terminal tab, settings/channels tabs, screenshot coverage, and focused runtime tests. Screenshot fixture now seeds a real Project-root README via the workspace file API.
- [x] **Phase 2B - Project membership** — Project detail Channels tab became actionable: create Project-bound channels, attach existing channels, detach members, open channel/settings, and screenshot coverage for the membership flow. Channel creation now accepts `project_id` directly.
- [x] **Phase 2C - WorkSurface interface** — Project-root policy is centralized behind `projects.WorkSurface`; context assembly, file tools, exec tools, channel search/knowledge search, channel indexing, harness cwd, and admin validation now consume the same root/prefix/prompt contract. Added architecture and regression tests to keep callers off the low-level root helpers.
- [x] **Phase 3A - Blueprints v0** — Project Blueprints are first-class reusable recipes with DB-backed CRUD, Project creation from a blueprint, starter folder/file/knowledge-file materialization, applied blueprint snapshots, repo/env declarations, and Project-level secret binding slots. v0 intentionally records repo/env/secret declarations without cloning repos, running setup, or injecting secret values into runtimes.
- [x] **Phase 3B - Blueprint management surface** — Blueprints now have dedicated admin routes for library/detail editing, richer editors for starter files, knowledge files, repo declarations, env defaults, and required secrets, DELETE support that clears live references while preserving Project snapshots, and Project creation preview for selected recipes. The Project Workspace screenshot bundle now covers Blueprint library, editor, and applied-Blueprint settings artifacts.
- [x] **Phase 3C - Blueprint setup runs** — Project Setup now turns the applied Blueprint snapshot into a clone-only runtime plan with Project-scoped secret-slot readiness, persisted setup-run history, conservative repo path safety, existing-path skip behavior, and redacted logs. The Project detail surface has a Setup tab and the Project Workspace screenshot bundle has setup readiness/run-history specs queued for the e2e server once that instance has the current Project Blueprint routes.
- [x] **Phase 3D - Runtime environment** — Project runtime env now has a backend work-surface service, safe readiness API, Project Settings readiness card, setup-plan integration, Project terminal/exec injection, Codex/Claude harness env handoff, and runtime-value redaction across exec and harness event envelopes. Screenshot spec now asserts runtime readiness and no secret leakage; live e2e capture is waiting on the e2e backend deployment of `/api/v1/projects/{id}/runtime-env`.
- [x] **Phase 3E - Blueprint setup commands** — Project Blueprints can now declare ordered shell setup commands that run after repo preparation through Project Setup. Commands use Project-relative cwd, bounded timeouts, Project runtime env and secret bindings, redacted output, persisted run history, and screenshot coverage in the Blueprint editor and Setup tab. E2E staging passes; full screenshot recapture is waiting on the e2e backend deployment of `setup_commands`.
- [x] **Phase 4A - Fresh Project instances** — Added `project_instances`, session/task bindings, creation/list APIs, shared work-surface resolution for instance roots, task-run opt-in via `execution_config.project_instance.mode=fresh`, session binding APIs, a Project Instances tab, task form work-surface controls, docs, and screenshot staging/spec coverage. Live e2e screenshot staging is waiting on the e2e backend deployment of `/api/v1/projects/{id}/instances`.
- [x] **Phase 4B - Session fresh workspace control** — Added a readable session Project-instance state API, session summary fields, composer-level work-surface control, split-pane session menu actions, and screenshot assertion coverage for the Project-bound channel composer. The UI deliberately avoids header chips; session work-surface state lives near the command surface that will use it.
- [x] **Phase 4C - Agent coding runs v0** — Fresh Project instances now flow through the same work-surface policy as shared Projects for file/exec/harness/context/index behavior. Added Project coding-run presets, Project run receipts, a `publish_project_run_receipt` bot tool, Project receipt API/UI, Agent Doctor coding-run readiness, and screenshot bundle coverage for the Runs tab.
- [x] **Phase 4D - Real shared-root smoke** — Ran a main-server Project-bound agent task through the shared `common/projects` root. The agent fast-forwarded `mtotho/spindrel`, created a text-only fixture branch, opened draft PR #102, and published Project run receipts. Screenshot evidence now includes the refreshed Project Workspace bundle plus main-server smoke channel/runs captures.
- [x] **Phase 4E - Coding-run reliability pack** — Project run receipts are idempotent by task/handoff/git metadata or explicit key, the Project Runs UI collapses duplicate review receipts inline, and `run_e2e_tests` resolves the configured e2e target instead of assuming container-local `localhost:18000`. Refreshed the Project Workspace screenshot bundle against the updated `:18000` e2e API with local UI changes, including memory-tool envelope verification.
- [x] **Phase 4F - Guided coding-run launch surface** — Added the Project coding-run API/service around the `project_coding_run` preset. Launch now validates Project channel membership, generates a branch/base/repo/runtime handoff, stores it in task `execution_config.project_coding_run`, creates a normal pending task with fresh-instance/new-session policy, and exposes `/coding-runs` as the Runs tab review cockpit with task, branch, activity, receipt, and handoff links. After deployment, the 18000 e2e server returned 200 for `/coding-runs`, staging created a real API-launched run, and the Project Workspace screenshot bundle passed against the deployed e2e UI.
- [x] **Phase 4G - Coding-run branch/PR handoff** — Added the `prepare_project_run_handoff` service/tool so Project coding agents can prepare the configured branch, push work, and open/reuse a PR while publishing `project_coding_run` execution receipts. The Runs tab now surfaces branch/PR progress from those receipts, screenshot staging seeds handoff progress for the Project Workspace bundle, and the 18000 e2e server was confirmed on `development` at `ab6327bd` before recapturing `13/13` Project Workspace screenshots.
- [x] **Phase 4H - Coding-run review cockpit** — Project coding runs now compute reviewer state from task, handoff, PR status, receipts, evidence, and instance state. Added refresh, mark-reviewed, and task-owned instance cleanup actions, plus a low-chrome Runs row that exposes review readiness, PR/check progress, evidence counts, handoff links, and cleanup controls. The Project Workspace screenshot bundle was recaptured against local UI plus the updated `:18000` e2e server at `e90e61d6`; the memory-tool screenshot still shows the structured tool result envelope.
- [x] **Phase 4I - Coding-run continuations** — Reviewers can now request changes on an existing Project coding run. The continuation endpoint creates a linked follow-up task on the same Project/channel/repo/branch/PR, stores parent/root lineage plus reviewer feedback and prior evidence in the task execution config, and prompts the agent to update the existing PR and publish a new receipt. The Runs tab shows Request changes, continuation counts, and latest follow-up status. The Project Workspace screenshot bundle passed against local UI and rebuilt `:18000` e2e at `b34c6afa`.
- [x] **Phase 4J - WorkSurface basics hardening** — Re-audited the Project track and closed foundation gaps before more factory work. Project resolver failures now fail visibly, active fresh instances must belong to the channel Project, Project-bound file and exec paths are clamped to the active WorkSurface root, channel-bound harnesses use the resolved WorkSurface before `harness_workdir`, the normal channel settings UI no longer exposes path-only Project config, and Project Settings now has a compact Basics readiness block.
- [x] **Phase 4K - Agent-run factory guardrails** — Added the first durable pieces for Project coding agents that need real machine/e2e access: task-scoped SSH machine grants, `machine_inspect`/`machine_exec` pipeline steps behind those grants, task-context propagation for machine-control tools, always-injected agent self-inspection/Doctor tools, richer skill-opportunity manifest metadata, deterministic planning-loop repair for narrow small-model slips, and exact-preview guards for spatial widget mutations.
- [x] **Phase 4L - Batch review and merge handoff** — Project Runs now supports selected-run review batches, manual batch-reviewed receipts, and Project review sessions as normal tasks with `project_coding_run_review` config. Review agents get Sandcastle-style `!` prompt expansion scoped to Project review templates, a constrained `finalize_project_coding_run_review` tool that validates selected run ids, accepted-only reviewed semantics, optional PR merge via `gh pr merge`, and review/merge provenance back on each run row.
- [x] **Phase 4M - Review-agent hardening** — Project review agents now have a runtime `workspace/project_coding_runs` skill and a read-only `get_project_coding_run_review_context` tool that returns selected runs, evidence, handoff URLs, runtime/e2e/GitHub readiness, and finalization rules from fresh server state. The review finalizer returns structured error envelopes for invalid selections and merge blockers, and Agent Capabilities/Doctor readiness now includes review-context and finalizer tool coverage. The `:18000` e2e screenshot instance was updated to current `development`; Project Workspace capture now passes `15/15`, including review-session launch and accepted review/merge provenance, and a live Codex project-build e2e against main `:8000` passed with terminal/context/plan-mode screenshots.
- [x] **Phase 4N - Task-scoped execution access** — Project coding-run and review-session launches can now attach explicit task-scoped machine/e2e grants backed by the task machine grant table instead of relying on ambient Project power. Prompts, execution config, run rows, and review context expose only secret-safe provider/target/capability summaries; the Project Workspace screenshot bundle now covers coding-run execution access and review execution access and passes `17/17`.
- [x] **Phase 4O - Issue Work Packs** — Added the first Issue Intake to Project Factory bridge. Conversational bots can publish rough issue notes through `publish_issue_intake`; Operator issue-triage tasks can publish durable `IssueWorkPack` rows through `report_issue_work_packs`; Mission Control Review has an Issues lane for raw notes, generated packs, Project/channel launch targets, and launch into normal Project coding runs with `source_work_pack_id` provenance. Screenshot coverage now includes the Issue intake/work-pack lane inside the spatial-check bundle.
- [x] **Phase 4P - Scheduled Project coding runs** — Project Runs now has recurring Project coding-run schedules backed by ordinary Task schedule rows with a distinct `project_coding_run_schedule` preset marker. Scheduler fires create fresh concrete Project coding runs through the same launch path, preserve schedule provenance on the run task, keep schedule definitions out of the concrete run list, and support Run now without moving the next scheduled fire. Added a bot-facing `schedule_project_coding_run` tool, runtime skill guidance, Agent Doctor/tool readiness coverage, API hooks, a low-chrome Scheduled Reviews section, and a new Project Workspace screenshot artifact for scheduled review controls.
- [x] **Phase 4Q - Spatial Project planets** — Projects with attached channels now have first-class Spatial Canvas nodes. Project planets group member channel/widget constellations for movement, appear in map-state/Starboard/palette surfaces, and Project coding-run schedules render as Project satellites while remaining ordinary `Task` schedule rows.
- [x] **Phase 4R - Agent E2E development playbook** — Locked the execution boundary for Project Factory agents: local repo-dev agents can run e2e/screenshot scripts directly, normal Spindrel bots use runtime `e2e_testing` and `run_e2e_tests`, and Codex/Claude harness agents use native tools for Project-root code work while e2e, screenshots, server/machine, Docker/compose, finalization, and receipts go through task-granted Spindrel tools. Added the public Agent E2E Development guide, repo-dev skill, runtime skill updates, prompt preset guidance, local helper for fresh e2e env/auth/screenshot setup, compose override support for Codex/Claude auth mounts, explicit subscription-mode handoff, and a local fresh-stack health smoke.

## Queued Follow-Ups

- Fresh instances: add expiration policy/worker cleanup for old task-owned instances after manual cleanup has settled.
- Blueprint setup observability: split clone/command run phases into clearer progress events and add rerun controls after deployed E2E validates setup-command history.
- Agent coding runs: add rerun controls for failed review steps, background expiration cleanup for task-owned instances, and optional GitHub status webhooks/polling beyond explicit refresh.
- Project factory: add a review-session summary list/filter so overnight review tasks can be audited separately from individual run rows.
- Project factory: add manual Work Pack edit/dismiss controls and a reviewable triage-run receipt before scaling scheduled triage.
- Project factory: add schedule edit/resume controls and schedule-run history filters once recurring review usage settles.
