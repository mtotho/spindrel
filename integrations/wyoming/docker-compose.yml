name: "spindrel-${SPINDREL_INSTANCE_ID:-default}-wyoming"

services:
  whisper:
    image: rhasspy/wyoming-whisper
    ports:
      - "10300:10300"
    command: --model tiny-int8 --language en
    restart: unless-stopped

  piper:
    image: rhasspy/wyoming-piper
    ports:
      - "10200:10200"
    command: --voice en_US-lessac-medium
    restart: unless-stopped
